# Initial Statistics

tmp_ap_0.txt is taken as of 1bc3a0825a85b4e18120d872ef93e418e59e18ca in csl-orig repository on Apr 9 23:10:32 2026 +0530

1           {@          15364
1.1         {@{         10909
1.1.1       {@{#        10909
1.1.1.1     {@{#\-      10489
1.1.1.1.1   [.]{@{#\-    8117
1.1.1.1.1.1 ^[.]{@{#\-   8117
1.1.1.1.2   [^.]{@{#\-   2380
1.1.1.1.2.1 [(]{@{#\-    2092
1.1.1.1.2.2 [ ]{@{#\-     308
1.1.1.1.2.3 [^( .]{@{#\-    1
1.1.1.2     {@{#[^-]      454
1.1.1.2.1   [ ]{@{#[^-]   280
1.1.1.2.2   [.]{@{#[^-]    19
1.1.1.2.3   ━{@{#[^-]      91
1.1.1.2.4   [(]{@{#[^-]    71
1.1.1.2.5   \[{@{#[^-]      1
1.2         {@<          4454
1.2.1       {@<ab>       4454
1.3         {@[^<{]         1

# Step1

subsumed under step 2. No separate script now.

# Step 2

Correct entries having '^[.]{@{#\-([^ }]+)#}@}' in the regex.
`python3 step2.py tmp_ap_0.txt tmp_ap_2.txt log2.tsv`
This takes tmp_ap_0.txt as input and produces tmp_ap_2.txt as output.
log2.tsv is a tab separated file with Lid, basehw, suffix and resolution fields. If resolution is not found, the answer is shown as 'None'.
It also uses manually_mapped.tsv as a fallback for cases where automatic resolution fails. This file contains manual mappings for cases that cannot be resolved automatically.
Where automatic resolution could not be done, tmp_ap_2.txt file has key1 and key2 marked with '.ABC' as placeholder, so that they can be easily identified and corrected manually.
As there is a high possibility that L numbers may change subsequent to handling of other patterns in future, all new L numbers are marked with '.XYZ' as placeholder in tmp_ap_2.txt. They will be mechanically filled in later, looking at the L numbers of surrounding entries.
Thus the result of `cat tmp_ap_2.txt | grep '.ABC' | wc -l` should be equal to `cat log2.tsv | grep 'None' | wc -l`. Both should be 0 after incorporating manually_mapped.tsv.
`cat tmp_ap_2.txt | grep '<L>.*\.XYZ' | wc -l` should be equal to `cat log2.tsv | wc -l` minus one (the header).
There are additional `{{Lbody=*.XYZ}}` lines added for entries with multiple suffixes. So total `cat tmp_ap_2.txt | grep '.XYZ' | wc -l` = (`cat log2.tsv | wc -l` - 1) + (number of multi-suffix entries).

# Step 3

Check whether resolution column of log2.tsv exists in sanhw1.txt (a list of valid Sanskrit words).
`python3 step3.py`
This reads log2.tsv and produces log3.tsv as output with an additional column 'in_sanhw1' which is True or False.
Additionally, if the resolution ends with 'H' or 'm', it strips that character and checks again in sanhw1.txt for a match.

## manually_mapped.tsv

A total of 383 mappings were manually added to resolve cases where automatic resolution failed. This file was found by cases where log2.tsv was showing 'None' in the resolution column. Then the resolution was manually provided. This file is now also made into a fallback mechanism to step2.py. Thus, step2.py now generates resolution for all entries.

## log3.tsv

This file is generated by step3.py. It contains the resolution column from log2.tsv and an additional column 'in_sanhw1' which is True or False. If the resolution ends with 'H' or 'm', it strips that character and checks again in sanhw1.txt for a match. As on 19 April 2026, there are 8518 entries in log3.tsv. Out of these, 8273 entries have 'in_sanhw1' as True and 245 entries have 'in_sanhw1' as False.

# Step 4

Replace the temporary `.XYZ` suffixes in `L` numbers with permanent incrementing values and resolve `{{Lbody=...}}` references.
`python3 step4.py`

This script takes `tmp_ap_2.txt` as input and produces `tmp_ap_4.txt` as output.

### Key Logic

1. **L Number Resolution**: Every `L` number ending in `.XYZ` is replaced with an incrementing even numeric suffix (e.g., `29.002`, `29.004`).
2. **Conflict Prevention**: The script pre-scans the file to identify all existing `L` numbers. Newly assigned numbers are checked against this set to ensure no duplicates are created.
3. **Lbody Mapping**: Temporary `{{Lbody=base.XYZ}}` references are updated to point to the numeric `L` assigned to the first entry of their respective suffix group.

### Statistics for tmp_ap_4.txt

- **Total occurrences of XYZ resolved**: 8,919
- **New headword entries created with numeric L**: 8,518
- **Lbody references updated**: 401
- **Total headword entries (<L> tags)**: 88,333
- **Mapping details**: Recorded in `log4.tsv`.

# L Number Comparison: tmp_ap_0.txt vs tmp_ap_4.txt

This section summarizes the changes in headword entries (represented by `<L>` tags) between the original file and the final processed file.

## 1. Summary Statistics

| Metric | tmp_ap_0.txt (Original) | tmp_ap_4.txt (Final) | Change |
| :--- | :--- | :--- | :--- |
| **Total Headword Entries** | 79,815 | 88,333 | +8,518 |
| **Numeric Suffixes (e.g. .2)** | ~6,000* | 14,518** | +8,518 |

*\* Approximate count of entries that already had numeric sub-entries (like 4.020).*
*\** Includes pre-existing sub-entries plus the 8,518 newly separated entries.

## 2. Explanation of the Increase

The increase of **8,518** headwords comes specifically from the separation of "hidden" headwords located within `.{@{#-suffix#}@}` blocks in the original entries.

- **Step 2** identified these suffixes and created new entries with the placeholder `<L>base.XYZ`.
- **Step 4** resolved these placeholders into unique, incrementing permanent `L` numbers (e.g., `29.002`, `29.004`).

## 3. Example of L Number Evolution

| Entry Type | Original (tmp_ap_0.txt) | Final (tmp_ap_4.txt) |
| :--- | :--- | :--- |
| **Parent Entry** | `<L>29` | `<L>29` (Unchanged) |
| **Suffix 1 (-hu)** | *Hidden inside L 29* | `<L>29.002` (New) |
| **Suffix 2 (-Beda)** | *Hidden inside L 29* | `<L>29.004` (New) |
| **Suffix 3 (-dI)** | *Hidden inside L 29* | `<L>29.006` (New, refers to 29.004) |

## 4. Derived Entry References

In `tmp_ap_4.txt`, many of the new entries use the `{{Lbody=...}}` tag to refer back to a shared definition, reducing redundancy.

- **Total Lbody references created**: 401
- These references allow multiple headwords (like `-Beda` and `-dI`) to share the same descriptive text while having distinct `L` identifiers for indexing.

# Merger

Once the suffixes are resolved and we have tmp_ap_4.txt file, there were also changes which happenned in csl-orig/v02/ap/ap.txt file.
To merge those changes the following was done

cd sanskrit-lexicon/csl-orig/
git merge-file v02/ap/ap.txt ../AP/issues/issue16/tmp_ap_0.txt ../AP/issues/issue16/tmp_ap_4.txt

This generated a file v02/ap/ap.txt.txt which has changes of both v02/ap/ap.txt and tmp_ap_4.txt merged. There were three cases where both the changes happenned in the same lines. They can be identified by searching for >>>>>. These three cases were manually resolved and the final file was saved as v02/ap/ap.txt.
